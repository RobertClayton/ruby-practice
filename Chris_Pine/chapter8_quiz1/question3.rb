# There is a program below.
# Add more code so that the program prints out each number in the array on a separate line.
# The array should be printed out in reverse order (starting at 8).
# The code must use the `each` method.

numbers = [5, 6, 7, 8]

numbers.reverse.each { |x| puts x }
